services:
  - type: web
    name: sweet-shop-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn sweetshop.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: sweetshop.settings
      - key: PYTHON_VERSION
        value: 3.11
